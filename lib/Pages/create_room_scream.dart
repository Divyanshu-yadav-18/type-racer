import 'package:flutter/material.dart';

class CreateRoomScream extends StatelessWidget {
  const CreateRoomScream({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Create Room'),
      ),
    );
  }
}
